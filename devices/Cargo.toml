[package]
name = "devices"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1.12.3"
thiserror = "2.0.18"
core = { path = "../core/" }
macros = { path = "../macros/" }
crossbeam = { version = "0.8.4", features = ["crossbeam-channel"] }
